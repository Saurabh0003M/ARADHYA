# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0

# Logging
loguru>=0.7.0
colorama>=0.4.6  # For colored terminal output on Windows

# Rich CLI experience
rich>=13.7.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Optional stacks live in separate files so a fresh clone keeps a lightweight
# install path. See requirements-voice.txt for Faster-Whisper support and
# requirements-windows.txt for future tray/hotkey packages.
