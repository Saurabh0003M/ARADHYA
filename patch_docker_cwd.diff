--- src/aradhya/tools/shell_tools.py
+++ src/aradhya/tools/shell_tools.py
@@ -66,6 +66,7 @@
         # Sandboxing support (ZeroClaw feature)
         import shlex
         final_command_args = shlex.split(command)
+        is_sandboxed = False

         if getattr(prefs, "use_docker_sandbox", False):
             try:
@@ -83,6 +84,7 @@
                     "python:3.10-slim",
                     "bash", "-c", command
                 ]
+                is_sandboxed = True
             except subprocess.CalledProcessError:
                 # Docker not installed/running, gracefully fallback to local execution
                 pass
@@ -93,7 +95,7 @@
             shell=False,
             capture_output=True,
             text=True,
-            cwd=str(work_dir) if "docker" not in final_command_args else None,
+            cwd=None if is_sandboxed else str(work_dir),
             timeout=timeout,
         )
         wall_time = time.perf_counter() - start_time
