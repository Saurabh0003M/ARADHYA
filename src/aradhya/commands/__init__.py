"""CLI command handlers, split out of the monolithic main.py.

Each module groups the handlers for one command family. main.py owns the
COMMAND_TABLE and dispatch; handlers here are imported and referenced there.
"""
